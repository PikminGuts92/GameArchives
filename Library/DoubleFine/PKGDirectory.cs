using GameArchives.Common;

namespace GameArchives.DoubleFine
{
  public class PKGDirectory : DefaultDirectory
  {
    public PKGDirectory(IDirectory parent, string name) : base(parent, name) { }
  }
}
